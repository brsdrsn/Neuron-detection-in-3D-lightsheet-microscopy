# Core pipeline dependencies
# Install into a fresh venv, then: pip install -e ./napari-large-tiler

napari>=0.5.0
magicgui>=0.8.0
dask[array]>=2024.1.0
zarr>=2.16.0
numpy>=1.24.0
tifffile>=2024.1.0
pandas>=2.0.0
matplotlib>=3.7.0
Pillow>=10.0.0
ultralytics>=8.0.0
